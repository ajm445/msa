# ===========================================
# MSA Analyzer Backend 환경 변수 설정
# ===========================================
# 이 파일을 복사하여 .env 파일을 생성하세요:
# cp .env.example .env

# -------------------------------------------
# 서버 설정
# -------------------------------------------
PORT=3000
NODE_ENV=development

# -------------------------------------------
# 프론트엔드 URL (CORS)
# -------------------------------------------
FRONTEND_URL=http://localhost:5173

# -------------------------------------------
# Supabase 설정 (필수)
# -------------------------------------------
# Supabase 프로젝트 URL
# https://supabase.com/dashboard > Project Settings > API 에서 확인
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (공개 키)
# Project Settings > API > Project API keys > anon public
SUPABASE_ANON_KEY=your_supabase_anon_key

# -------------------------------------------
# Claude API 설정 (AI 분석용)
# -------------------------------------------
# https://console.anthropic.com/settings/keys 에서 발급
ANTHROPIC_API_KEY=your_anthropic_api_key

# Claude API 비용 제한 (선택)
# 월간 총 한도 (달러)
CLAUDE_MONTHLY_LIMIT=10
# 경고 임계값 - 이 금액 이상 사용 시 API 호출 거부 (달러)
CLAUDE_WARNING_THRESHOLD=5

# -------------------------------------------
# Voyage AI 설정 (RAG 임베딩용)
# -------------------------------------------
# https://dash.voyageai.com/api-keys 에서 발급
VOYAGE_API_KEY=your_voyage_api_key
